<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/customColorPrimary</item>
        <item name="colorPrimaryDark">@color/customColorPrimaryDark</item>
        <item name="colorAccent">@color/customColorAccent</item>
    </style>
    <!--TabLayout theme-->
    <style name="AppTheme.Tab"/>
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="android:background">@color/customTabBackground</item>
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/customSelectedTabText</item>
    </style>
    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/customTabText</item>
        <item name="textAllCaps">true</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="quickContactBadgeStyle">
        <item name="android:background">#D8D8D8</item>
    </style>
    <style name="ContactListView">
        <!--<item name="android:verticalScrollbarPosition">right</item>-->
        <!--<item name="android:fastScrollAlwaysVisible">true</item>-->
        <!--<item name="android:scrollbarStyle">outsideInset</item>-->
        <item name="android:scrollbars">vertical</item>
    </style>
    <style name="searchTextHighlight">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
